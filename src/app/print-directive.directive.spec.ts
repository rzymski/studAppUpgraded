import { PrintDirectiveDirective } from './print-directive.directive';

describe('PrintDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PrintDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
